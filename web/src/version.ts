/**
 * Версия фронта — источник правды для VersionBadge.
 * Инкрементируй version (semver, patch за фичу) и обнови note (2-3 слова)
 * при каждой новой фиче, чтобы по бейджу в углу UI было видно, что реально
 * выкачено на сервере.
 */
export const APP_VERSION = {
  version: '0.0.9',
  note: 'редактирование сообщений',
}
