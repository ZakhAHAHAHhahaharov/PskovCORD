// Тонкая обёртка: пока без нативных мостов.
// Сюда позже — трей, системные уведомления, глобальные хоткеи push-to-talk и т.п.

// Метка «это НАШЕ настольное приложение» для отчётов об ошибках
// (web/src/errorTransport.ts). По User-Agent это не определить: строка
// Electron/ есть у любого приложения на Electron, в том числе у сторонних
// браузеров-обёрток, и веб-версия, открытая в таком, засчитывалась бы как
// наш десктоп. Флаг ставит только наш preload, поэтому врать он не может.
window.__PSKOVCORD_DESKTOP__ = true

window.addEventListener('DOMContentLoaded', () => {
  // no-op
})
